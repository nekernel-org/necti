export ncpp.ostream;

#include <stdio.h>

struct iostream
{
    void consume(int& val)
    {
        printf("%i", val);
    }

    int read()
    {
        return getchar();
    }
};

typedef iostream* ostream_ptr;

ostream_ptr out;
ostream_ptr in;


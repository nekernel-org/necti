#!/bin/sh

if [ -z "$1" ]
  then
    echo "nectar: No argument supplied"
fi

for index in "$@"
do
  cpp -P $index.nc -o $index.pp.nc
  pef-amd64-necdrv -fuse-nasm $index.pp.nc
  echo $index.nc.pp
done
name: Ne.app Nectar Kits Linux (Nectar, Develop)

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Attempt NeBuild
      run: sudo curl https://github.com/ne-app-eu/bld/releases/download/v0.0.6-apple-m1/bld-debian -o sanity-test
    - name: Build CompilerKit and DebuggerKit
      run: sudo curl https://github.com/ne-app-eu/bld/releases/download/v0.0.6-apple-m1/bld-debian -o /bin/bld && sudo chmod +x /bin/bld && sudo apt update && sudo apt install build-essential && sudo apt install libboost-dev &&
          cd private/src/CompilerKit && nebld ck-posix-san.json
